fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.7.0
httpx==0.28.1
alembic==1.19.1
# Postgres driver. The binary wheel avoids needing libpq and a compiler in the
# build image; swap for plain psycopg to link against the system libpq instead.
psycopg[binary]==3.3.5
# Required by FastAPI to parse HTML form submissions.
python-multipart==0.0.20
